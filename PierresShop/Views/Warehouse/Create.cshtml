@{
  Layout = "_Layout";
}
@model PierresShop.Models.Warehouse
<div class="container">
  <div class="col-md-12">
    <div class="row">
      <div class="card-header">
        <h1>Add a new warehouse</h1>
      </div>
      <br>
      @using (Html.BeginForm())
      {
        @Html.LabelFor(model => model.Name)
        @Html.TextBoxFor(model => model.Name, new { @required = "required"})
        @Html.Label("Inventory?")
        @Html.DropDownList("InventoryId")
        <input id="button" type="submit" value="Add new warehouse" class="btn btn-success" />
      }
      <button id="button" type="button" class="btn btn-success">@Html.ActionLink("Warehouse list", "Index")</button>
    </div>
  </div>
</div>